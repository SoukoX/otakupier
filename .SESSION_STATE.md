# OtakuPier — Session State (packed up Aug 10, 2026)

## Deployed & Live
- GitHub Pages: `https://soukox.github.io/otakupier/` (repo `SoukoX/otakupier`, branch `main`)
- Custom domain: `https://otakupier.2bd.net` — DNS A record `185.199.108.153` (GitHub Pages), verified resolving on all resolvers
- TLS cert LIVE: `CN=otakupier.2bd.net`, Let's Encrypt, valid Aug 10 → Nov 8 2026, HTTPS returns 200

## Aug 11 update — code issues fixed & pushed
- All canonical/OG/JSON-LD/sitemap/robots/config URLs switched from `soukox.github.io/otakupier` → `https://otakupier.2bd.net` (21 files pushed)
- Invalid CAPTCHA key cleared in `js/config.js` (`1307338918xyzbb.com` was a SECRET key; now `""` = forms work without CAPTCHA)
- `pass.txt` (contained a password) was accidentally pushed, then deleted from the repo — **still in git history, treat as compromised**
- Push tool restored at `/home/ac/push_update.py` (token via `GH_TOKEN` env, respects `.gitignore`): `GH_TOKEN=<token> python3 /home/ac/push_update.py "message"`

## Still Pending (user-side actions)
1. **Verify HTTP→HTTPS redirect**: Enforce HTTPS ticked; http:// still returns 200 without 301 — GitHub can take up to 24h for custom domains.
2. **DNS check banner**: GitHub Settings→Pages may show "DNS check in progress" — GitHub UI lag; DNS/cert/HTTPS verified. If stuck for hours, add HTTPS record in DNSExit: `1 . alpn="h2,h3"`.
3. **Google Search Console**: submit new domain `otakupier.2bd.net`, re-submit sitemap for new URL.
4. **Rotate the leaked password** that was in `pass.txt` (`i0dT!h5YSfo42tUV`) wherever it is used (likely Supabase DB password).
5. **CAPTCHA**: client now sends no token (key cleared). If Supabase "Bot and Abuse Protection > CAPTCHA" is enabled, signup/signin will fail until disabled or a real Turnstile site key is set.

## Live Features (all pushed & working)
- Catalog with parallel fetch + silent skip of failed Jikan pages
- api.js: retries 3x350ms, `_cache`/`cachedGet`, `getAnime` `/full`→`/` fallback, search falls back to client-side top-list filter
- Back buttons (anime/character) via `goBack()` (document.referrer)
- Friends: find by username + request buttons
- Chat 24h reset (client filter + `prune_chat_messages` RPC)
- Votes: 1 per non-admin user, admins unlimited
- SEO: meta/OG/JSON-LD/canonical on all pages, `favicon.svg`, `images/banner.svg`, `robots.txt`, `sitemap.xml` (valid, all 8 URLs 200)
- Brand is **OtakuPier** (AniVerse rebrand reverted)

## Notes
- Push with: `python3 /tmp/opencode/push_update.py "message"`
- Jikan upstream flaky: `/top/anime` pages 3+ fail, search endpoint down (MAL outage)
- Supabase: `https://ujblssvczuzsnbxajxwc.supabase.co`, anon `sb_publishable_-WgiSVvnk1_NA4nfdO0IPQ_EqkevAMl`
- `CONFIG.SITE_URL` + all canonical/OG/sitemap URLs now point to `https://otakupier.2bd.net`
- `otakupier.com` verified available if user wants a real domain later
